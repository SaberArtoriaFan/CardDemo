# Unity 迁移交接文档

## 1. 文档目的

这份文档用于把当前 `html` 卡牌原型整理成一份可直接交给后续 AI 或开发者接手的 Unity 迁移说明。

目标是明确四件事：

- 当前 HTML 原型已经实现了什么
- 用户当前真正想要的目标规则是什么
- 现状实现和目标设计之间有哪些差异
- Unity 项目应该按什么顺序落地

## 2. 仓库现状

### 2.1 HTML 原型现状

当前可玩的原型位于 `html/`，关键文件如下：

- `html/index.html`：战斗页
- `html/main.js`：核心战斗逻辑
- `html/keywords.js`：词条注册
- `html/tools.html`
- `html/tools.js`：DIY 配置工具页
- `html/prototype-mvp-plan.md`
- `html/demo-acceptance.md`

这一套 HTML 原型已经足够抽取出：

- 卡牌数据结构
- 回合状态机
- 场上牌序逻辑
- 同步锁定操作后再统一结算的流程
- 顺序结算规则
- 简单敌方 AI
- 本地配置与自定义卡格式

### 2.2 Unity 工程现状

Unity 工程位于 `Unity/`。

当前确认信息：

- Unity 版本：`2023.2.20f1`
- 渲染管线：URP 2D
- `Unity/Assets/` 下除了 `TcgEngine` 和默认场景资源外，还没有项目自己的玩法代码
- 仓库里已经有 `TcgEngine` 资源，但当前不应默认视为“玩法已接入”

建议理解为：

- 现在的 Unity 工程仍然是一个玩法空壳
- 第一目标是把 HTML 原型验证过的核心规则迁过去
- 等规则跑稳后，再决定是否深度适配 `TcgEngine`

## 3. 目标玩法定义

这一节以用户在 2026 年 8 月 9 日补充的最新玩法说明为准，优先级高于旧原型文档中的历史表述。

### 3.1 核心循环

每回合流程：

1. 双方同时布置或调整卡牌。
2. 双方确认后，从头到尾按顺序结算整条牌链。
3. 生命值和能量跨回合保留。
4. 重复直到一方生命值归零。

胜负条件：

- 率先生命值归 `0` 的一方失败

### 3.2 基础牌类型

第一版基础只有三种牌：

- `充能`
- `攻击`
- `防御`

### 3.3 基础规则

- `充能`：结算时获得能量；如果被攻击命中，仍然会失去生命值
- `攻击`：消耗能量发动攻击；攻击强度一般由本次消耗能量决定
- `防御`：若正对攻击，则免疫这次攻击伤害
- 能量跨回合保留

### 3.4 场上牌序规则

目标规则如下：

- 第 1 回合：双方各放 `3` 张牌
- 之后：每回合再放 `1` 张，直到场上最多 `6` 张
- 未满 `6` 张前，每次放牌都可以任选位置：
  - 最前
  - 最后
  - 中间插入
- 满 `6` 张后，不再新增放牌
- 满 `6` 张后，每回合只能二选一：
  - 调整 `1` 张牌的顺序
  - 抽/选 `1` 张牌替换场上 `1` 张牌

### 3.5 卡牌组成

每张牌由三部分组成：

- `类型`
- `消耗`
- `词条`

说明如下：

- 类型：充能 / 攻击 / 防御
- 消耗：这张牌会消耗多少能量
- 词条：打出时或结算时触发的特殊效果

## 4. 当前 HTML 原型里已经实现的内容

这一节描述的是 `html/main.js` 里真实存在的行为，不是仅存在于设计稿中的想法。

### 4.1 已实现基础卡

当前内置卡模板有：

- `charge`
- `insight-charge`
- `attack-1`
- `attack-2`
- `strong-attack-2`
- `attack-3`
- `defend`
- `recycle-defend`

当前原型中的卡牌字段结构：

```js
{
  key,
  name,
  type,
  cost,
  power,
  keywords,
  description
}
```

### 4.2 已实现词条

当前 `html/keywords.js` 里已注册并在战斗逻辑里真正落地的词条只有：

- `strong`
- `recycle`
- `insight`

现有含义：

- `strong`：对位也是攻击时，本次攻击强度 `+1`
- `recycle`：对位不是攻击时，额外获得 `1` 点能量
- `insight`：放下时揭示 `1` 张敌方已部署但仍隐藏的卡

### 4.3 已实现状态机

HTML 原型当前使用的阶段：

- `planning`
- `ready`
- `resolving`
- `roundEnd`
- `ended`

这套状态机可以直接作为 Unity 版状态设计参考。

### 4.4 已实现玩家操作

当场上未满 `6` 张时：

- 玩家每回合只能完成 `1` 次放牌操作
- 支持任意插入位置
- 支持点击放牌
- 支持拖拽放牌

当场上达到 `6` 张时：

- 玩家必须二选一：
  - `reorder`
  - `replace`
- 每回合只允许一次操作

### 4.5 已实现敌方行为

当前原型已经有一套轻量 AI，具备以下行为：

- 用权重方式选择插入位置
- 估算某个结算位之前的可用能量
- 能量低时更偏向充能
- 会根据玩家本回合动作类型做简单反应
- 满 `6` 张后会尝试换序或替换

这套 AI 的定位应该理解为：

- 足够做玩法陪测
- 不是正式 PvE 设计

### 4.6 已实现结算模型

当前 HTML 原型的结算顺序是：

1. 双方先各完成本回合的那 `1` 次操作
2. 阶段进入 `ready`
3. 玩家点击开始结算
4. 按位置从 `0` 到 `5` 顺序结算
5. 每个位置取玩家牌和敌方牌进行对位
6. 攻击在“轮到该位置结算时”才检查能量是否足够
7. 伤害处理后再应用充能/回收等被动结果
8. 任意一方生命值归零则立刻结束对局

当前已经验证过的重要行为：

- 前面的充能可以供给后面的攻击
- 敌方暗牌会在揭示或结算时逐步翻开
- 牌桌跨回合保留
- 每个位置结算后会留下结果提示

## 5. 当前原型与目标设计的差异

这一节非常重要，后续 AI 不能把“现有 HTML 行为”直接当成“最终规则”。

### 5.1 已确认差异

差异 1：首回合放牌数量

- 当前 HTML 原型：第 1 回合仍然是“本回合只做 1 次操作”
- 目标设计：第 1 回合双方各放 `3` 张牌

差异 2：词条范围

- 当前 HTML 原型只实现了 `strong / recycle / insight`
- 目标设计新增或提到了：
  - `破法`
  - `反击`
  - `转换`
  - `无效化`
  - `揭露`
  - `火车`

差异 3：洞察定义

- 当前 HTML 原型：洞察 = 揭示 `1` 张敌方已部署暗牌
- 目标设计：洞察 = 查看对手牌堆顶的一张牌

差异 4：满 6 张后的操作

- 当前 HTML 原型：满 `6` 张后只能换序或替换
- 目标设计：这一条方向一致，但应以后者作为正式规格来源

差异 5：长期系统

- 当前 HTML 原型只有战斗和配置工具
- 目标设计后续还要接入：
  - 双方独立牌堆
  - 玩家构筑
  - 遗物
  - PvE 信息优势

### 5.2 迁移时的规则优先级

建议后续开发遵循下面的优先级：

1. 用户在 2026 年 8 月 9 日给出的最新玩法说明
2. `html/main.js` 中已经被验证过的具体行为
3. `html/prototype-mvp-plan.md` 和 `html/demo-acceptance.md` 中的旧描述

如果 1 和 2 冲突：

- 文档里要显式标明
- 实现时优先靠近最新目标设计

## 6. Unity 迁移总策略

### 6.1 核心原则

不要把 HTML 的 DOM 代码直接翻译成 Unity 代码。

应该迁移的是：

- 规则
- 状态机
- 数据模型
- 结算流程

UI 需要在 Unity 中重新组织。

### 6.2 推荐架构

建议第一版采用“纯规则层 + Unity 展示层”结构：

1. `Data`
2. `State`
3. `Commands`
4. `Resolution`
5. `AI`
6. `UI`
7. `Persistence`

原则：

- 规则尽量写在可测试的纯 C# 类里
- MonoBehaviour 只负责驱动 UI 和派发命令
- 不要把核心战斗逻辑散在多个按钮回调里

### 6.3 场景建议

第一阶段只需要一个核心战斗场景：

- `BattlePrototype`

后续可再扩：

- `DeckBuilder`
- `Reward`
- `MetaMap`

## 7. 推荐的 Unity 目录结构

建议在 `Unity/Assets/` 下新增项目自己的模块目录：

```text
Assets/
  CardDemo/
    Scripts/
      Data/
      Core/
      State/
      Commands/
      Resolution/
      AI/
      UI/
      Persistence/
      Tests/
    ScriptableObjects/
      Cards/
      Keywords/
      Balance/
    Prefabs/
      Battle/
      UI/
    Scenes/
      BattlePrototype.unity
    Art/
      Cards/
      Icons/
```

如果后续要适配 `TcgEngine`，也建议先把这部分独立模块做出来，再决定怎么接。

## 8. Unity 里建议建立的数据模型

### 8.1 卡牌定义

推荐用 `ScriptableObject` 表示可复用卡牌模板。

建议字段：

```csharp
CardDefinition
- string Id
- string DisplayName
- CardType Type
- int Cost
- int Power
- List<KeywordType> Keywords
- string RulesText
```

### 8.2 运行时卡牌实例

运行时需要把“模板”和“实例”分开：

```csharp
RuntimeCard
- string InstanceId
- string TemplateId
- Side Owner
- CardDefinition Definition
- List<KeywordType> KeywordsSnapshot
```

原因：

- 换序和替换需要稳定身份
- 日志和回放需要能追踪具体那张牌
- 后续 buff/debuff 也需要实例级数据

### 8.3 单方状态

```csharp
SideState
- Side Side
- int Hp
- int MaxHp
- int Energy
- List<RuntimeCard> Board
- DeckState Deck
- List<RuntimeCard> Discard
- List<RevealInfo> RevealedInfos
```

### 8.4 对局状态

```csharp
BattleState
- int Round
- BattlePhase Phase
- BattleWinner Winner
- TurnMode? CurrentTurnMode
- SideState Player
- SideState Enemy
- List<BattleLogEntry> Logs
- List<SlotResult> SlotResults
- RoundSnapshot Snapshot
- SelectionState Selection
```

### 8.5 命令模型

不要让 UI 直接改状态，建议通过命令对象驱动。

```csharp
BattleCommand
- CommandType Type
- Side Actor
```

具体命令建议拆成：

- `PlaceCardCommand`
- `ReorderCardCommand`
- `ReplaceCardCommand`
- `ConfirmRoundCommand`
- `StartResolutionCommand`
- `NextRoundCommand`
- `ResetRoundCommand`

## 9. Unity 里要实现的结算规则

### 9.1 单个位置的推荐结算顺序

每个槽位建议按下面顺序结算：

1. 读取玩家牌和敌方牌
2. 计算双方是否能发动攻击
3. 如果双方都是攻击，先做对攻判定
4. 否则处理单边命中
5. 应用生命变化
6. 应用被动/后效
7. 写入本槽结果和日志

### 9.2 基础对位规则表

`充能 vs 攻击`

- 充能方在自己结算时获得能量
- 若攻击成功发动，充能方受到攻击伤害

`充能 vs 防御`

- 充能方获得能量
- 防御方没有实际挡到伤害

`充能 vs 充能`

- 双方各获得能量

`攻击 vs 防御`

- 防御免疫这次攻击伤害

`攻击 vs 充能`

- 若攻击成功发动，充能方受伤

`攻击 vs 攻击`

- 比较最终攻击强度
- 强度大的一方胜出
- 失败方受到胜方攻击值伤害
- 强度相同则双方互伤

`防御 vs 防御`

- 默认无额外效果

### 9.3 必须保留的时序规则

攻击牌是在“轮到该槽位结算时”才检查当前能量是否足够，不是在放牌时扣能量。

这一点必须在 Unity 中保留，因为它是当前玩法里最关键的策略点之一。

## 10. 词条系统设计建议

### 10.1 架构建议

不建议长期把所有词条都堆在一个巨大 `switch` 里。

推荐结构：

- `KeywordType`：稳定枚举 ID
- `KeywordTiming`：触发时机
- `KeywordRule`：具体规则处理器

建议时机枚举至少包含：

- `OnDeploy`
- `BeforeAttackCompare`
- `OnHit`
- `AfterResolve`
- `OnRoundPrep`

### 10.2 首轮迁移的词条范围

建议拆成两层。

第一层：优先做，尽快形成可玩闭环

- `Strong`
- `Insight` 或 `Reveal`
- `Recycle` 或 `Convert`

第二层：先预留结构，不一定在第一版 Unity 可玩包里全部完成

- `BreakMagic`
- `Counter`
- `Nullify`
- `Train`

### 10.3 用户最新描述下的词条含义

按目前需求说明，词条意图如下：

`强势`

- 如果对方也是攻击，则视为你成功
- 同时用户原文还写了“消耗能量 +1”
- 这一条和当前 HTML 里的“攻击强度 +1”并不相同，需要单独定规则

`破法`

- 命中成功后，扣除对方 `1` 点能量

`反击`

- 如果对方为攻击，反弹此次伤害

`转换`

- 如果对方为攻击，则获得 `1` 点能量

`洞察`

- 查看对手牌堆顶的一张牌

`无效化`

- 下 `X` 张牌的正对牌无效

`揭露`

- 随机揭露一张对方部署卡牌

`火车`

- 正序将卡组顺序推进 `X`

### 10.4 当前还不够明确的点

以下词条细则目前仍有歧义，后续 AI 不要擅自拍板：

- `强势` 到底是“自动赢对攻”还是“攻击强度 +1”还是两者都有
- `强势` 里的“消耗能量 +1”是额外扣 1 能量，还是代表强度提升 1
- `破法` 的“命中成功”是否包含对攻胜出
- `反击` 是单纯反伤，还是反伤且自身不受伤
- `无效化` 的 `X` 如何存储，影响的是槽位还是牌张数
- `火车` 影响的是自己牌序还是整条队列

## 11. 回合与场上流程设计建议

### 11.1 推荐阶段模型

Unity 版可使用如下阶段：

- `SetupRound`
- `Planning`
- `Ready`
- `Resolving`
- `RoundReview`
- `Ended`

如果想保留“双方同时行动”的体验：

- 敌方选择可以在玩家确认时后台完成
- 逻辑层仍然只在双方动作都锁定后进入 `Ready`

### 11.2 首回合特殊处理

目标规则要求第 1 回合双方各放 `3` 张牌。

建议实现方式：

- 给每回合一个“操作预算”
- 第 1 回合预算为 `3` 次放牌
- 第 2 回合起，如果场上未满 `6` 张，预算为 `1` 次放牌
- 场上满 `6` 张后，预算切换成 `1` 次换序或 `1` 次替换

不要把首回合强行拆成 3 个伪回合。

### 11.3 满 6 张后的阶段

当场上牌数达到 `6` 后：

- 不再新增放牌
- 每回合只允许做一次战术调整
- 然后仍然按整条 6 张链重新顺序结算

## 12. UI 从 HTML 到 Unity 的映射

### 12.1 战斗 HUD

建议把 HTML 上的信息区映射为 Unity UI：

- 顶栏：
  - 回合数
  - 当前阶段
  - 重开按钮
- 敌方区：
  - HP
  - 能量
  - 敌方牌桌
- 中央控制区：
  - 状态提示
  - 开始结算
  - 下一回合
  - 重置本回合
- 玩家区：
  - HP
  - 能量
  - 玩家牌桌
- 侧栏：
  - 当前操作模式
  - 可选卡牌列表
  - 当前选择详情
  - 战斗日志

### 12.2 交互建议

需要保留的核心交互：

- 点击手牌选牌
- 点击插入位置放牌
- 点击场上牌左右半边决定前插/后插
- 换序
- 替换

迁移优先级建议：

- 第一版 Unity 先做点击交互
- 拖拽交互放到第二阶段或作为可选优化

### 12.3 隐藏信息系统

当前原型支持敌方暗牌和局部揭示。

Unity 里建议最少支持：

- 敌方部署卡牌可隐藏
- 被揭示的卡单独显示
- 结算时或结算后整条链可翻开

第一版可以先用：

- `bool IsRevealed`
- 卡背图
- 文本遮罩

## 13. 配置与工具页迁移建议

### 13.1 当前 HTML 的持久化内容

当前原型通过本地存储保存：

- 玩家生命上限
- 敌方生命上限
- 玩家卡组 key 列表
- 敌方卡组 key 列表
- 自定义卡牌

### 13.2 Unity 中的推荐做法

第一版建议使用 JSON 做轻量配置保存：

- 战斗配置
- 卡组配置
- 自定义卡定义

可选位置：

- `Application.persistentDataPath`

不建议一开始就把完整工具页做成正式游戏 UI。

### 13.3 `tools.html` 在 Unity 中的替代方案

当前 `tools.html` 本质上是一个原型内容编辑器，负责：

- 改生命上限
- 配双方卡组
- 增删自定义卡
- 导入导出 JSON

在 Unity 迁移的第一阶段，更建议替代为：

- Inspector 配置
- 临时 Debug 面板
- 简单 JSON 导入

优先级顺序应是：

1. 先把战斗循环跑通
2. 再把数据从静态定义扩展为配置可读
3. 最后才考虑做工具界面

## 14. AI 迁移建议

当前 HTML 的 AI 已经足够当 Unity 版第一阶段陪测 AI。

建议分三步走：

阶段 1：

- 近似移植当前权重型 AI

阶段 2：

- 增加更清晰的局势判断和节奏策略

阶段 3：

- 再加入 PvE 信息差、敌人流派、卡背情报等内容

注意：

- 不要在核心战斗还没稳定前，先花太多时间调 AI

## 15. 推荐实施阶段

### 阶段 1：抽离纯规则层

目标：

- 先在 Unity 中写出不依赖场景的纯 C# 战斗核心

产出：

- 状态类
- 卡牌定义类
- 结算服务
- 日志结构
- 槽位结果结构

验收标准：

- 不开场景也能通过代码驱动一次完整结算

### 阶段 2：复现当前 HTML 可玩闭环

目标：

- 先在 Unity 中做出和当前 HTML 原型等价的可玩版

产出：

- 基础战斗 UI
- 插入放牌
- 满 6 张后的换序 / 替换
- 开始结算
- 回合结算复盘

验收标准：

- 能打一整局
- 牌桌跨回合保留
- 能量跨回合保留
- 槽位结果能展示

### 阶段 3：应用目标规则修正

目标：

- 从“复现 HTML 原型”升级到“符合最新玩法目标”

产出：

- 首回合双方各放 3 张
- 洞察改为查看牌堆顶（如果最终确认）
- 新词条的结构预留和部分实现

验收标准：

- Unity 版规则以最新设计说明为准，而不是只等价旧 HTML

### 阶段 4：为后续构筑和 PvE 做准备

目标：

- 给后续类杀戮尖塔方向预留扩展位

产出：

- 牌堆模型
- 奖励/遗物占位结构
- 敌人流派数据结构

验收标准：

- 后续可自然接入构筑和局外成长

## 16. 第一版 Unity 迁移的验收标准

第一版 Unity 迁移完成后，至少应满足：

- 能进入 Unity 战斗场景并完成对局
- 双方都有生命值和跨回合保留的能量
- 整条牌链按从左到右顺序结算
- `充能 / 攻击 / 防御` 三种基础牌可正常工作
- 攻击在结算到该槽位时才检查能量
- 牌桌跨回合保留
- 未满 `6` 张时支持任意位置插入
- 满 `6` 张后只能换序或替换
- 敌方能自动做出合法动作
- 日志能解释生命和能量变化原因
- 生命值归零时能正确结束

## 17. 主要风险

风险 1：把当前原型实现误当成最终设计

- 尤其是首回合放牌数、洞察定义、强势定义

风险 2：UI 直接修改战斗状态

- 会导致后续词条和规则扩展非常难维护

风险 3：过早追求拖拽体验

- 拖拽是体验优化，不是规则迁移的阻塞项

风险 4：太早深度绑死 `TcgEngine`

- 现阶段更重要的是先证明规则在 Unity 里跑通

风险 5：没有区分模板和实例

- 换序、替换、揭示、日志、后续 buff 都会依赖实例身份

## 18. 仍待确认的问题

这些点在正式实现进阶词条前，建议用户再确认一次：

1. `强势` 的最终规则到底是：
   - 自动赢对攻
   - 攻击强度 +1
   - 或两者都有
2. 首回合双方放 3 张时：
   - 是逐张布置并可见顺序
   - 还是一次性选完后统一锁定
3. `洞察` 是否最终一定改成“查看对手牌堆顶”
4. `无效化` 的持续范围和目标定义
5. `火车` 的准确位移规则
6. 满 6 张后的“替换”在原型阶段是自由选牌还是必须真的从牌堆抽牌

## 19. 推荐后续 AI 的第一步任务

建议下一位 AI 先按以下顺序推进：

1. 在 `Unity/Assets/CardDemo/Scripts/` 下创建纯 C# 战斗核心
2. 先实现卡牌定义、对局状态、结算服务
3. 先用点击交互复现 HTML 的战斗闭环
4. 再处理首回合 3 张放牌和新词条规则

## 20. 本文档对应的主要信息来源

仓库内抽取来源：

- `html/main.js`
- `html/keywords.js`
- `html/tools.js`
- `html/prototype-mvp-plan.md`
- `html/demo-acceptance.md`

目标规则来源：

- 用户在当前任务中于 2026 年 8 月 9 日补充的玩法说明

